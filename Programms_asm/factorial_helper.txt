Вводим число, хотим num!
Кладем его в RAX









FACTOR:
Если RAX == 1:
    \\ НЕТ кладем в стек 1
    возвращаемся из функции
Если RAX != 1:
    Кладем в стек RAX
    Вычитаем из RAX 1 (RAX -= 1)
    Вызываем функцию
    Кладем RAX
    Умножаем 2 последних элемента стека
    Кладем результат в RAX
    возвращаемся из функции